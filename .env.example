# Disposable local database only. Never copy the historical production URL.
DATABASE_URL=postgresql://demo:local-demo-only@127.0.0.1:5197/tofu_tracker
NEXTAUTH_URL=http://localhost:5196
# Generate locally. Do not commit the result.
NEXTAUTH_SECRET=
# Only needed to exercise real email sign-in; leave unset for offline tests.
EMAIL_API=
EMAIL_SECRET=

# Keep disabled until the complete visitor-scoping verification passes.
VISITOR_DEMO=false
NEXT_PUBLIC_VISITOR_DEMO=false
